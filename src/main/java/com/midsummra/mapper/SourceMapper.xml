<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.midsummra.mapper.SourceMapper">
    <select id="queryAllResource" resultType="com.midsummra.pojo.Source">
        select * from project_test.translation_resource
    </select>

    <insert id="addResource" parameterType="com.midsummra.pojo.Source">
        insert into project_test.translation_resource (resource, translation, date, likes) values (#{resource},#{translation},#{date},#{likes})
    </insert>

    <update id="addResourceLikes" parameterType="java.lang.String">
        update project_test.translation_resource set likes = likes+1 where translation = #{translation} and resource = #{resource}
    </update>
</mapper>