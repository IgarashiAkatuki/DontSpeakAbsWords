<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.midsummra.mapper.UserMapper">
    <select id="getUser" resultType="com.midsummra.pojo.User">
        select * from project_test.user where username = #{username}
    </select>

    <insert id="addUser" parameterType="com.midsummra.pojo.User">
        insert into project_test.user ( username, password, permission, date) values (#{username},#{password},#{permission},#{date})
    </insert>

    <update id="refreshDate">
        update project_test.user set date = #{date} where username = #{username}
    </update>
</mapper>